import {Generic} from "../generic/Generic";
import {IRectangleSettings} from "./IRectangleSettings";

export class Rectangle extends Generic {
    constructor(settings: IRectangleSettings) {
        super(settings);
    }
}
